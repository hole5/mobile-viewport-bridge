/** Re-export shared device presets — single source of truth in @mvb/shared. */

export type {
  DevicePreset,
  NotchType,
  HomeIndicatorType,
} from '@mvb/shared';

export { DEVICE_PRESETS } from '@mvb/shared';
